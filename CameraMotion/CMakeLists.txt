cmake_minimum_required(VERSION 2.8)
project("Camera motion")
find_package(OpenCV REQUIRED)
add_executable("Camera motion" CameraMotion.cpp OutputControl.cpp)
target_link_libraries("Camera motion" ${OpenCV_LIBS})
